import { SecureConfigView } from "./secureconfigView";
import { API_KIWI } from "@utils/Const/Api";

interface SecureConfigViewProps {

}

const SecureConfigContainer = (): JSX.Element => {

  return (
    <SecureConfigView/>
  );
};

export { SecureConfigContainer };
export type { SecureConfigViewProps };
